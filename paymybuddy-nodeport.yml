apiVersion: v1
kind: Service
metadata:
  name: paymybuddy
spec:
  type: NodePort
  selector:
    app: paymybuddy
    tier: webapp
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30088